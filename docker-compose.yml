version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: webbook-postgres
    environment:
      POSTGRES_DB: webbook
      POSTGRES_USER: webbook
      POSTGRES_PASSWORD: webbook
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U webbook"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:

